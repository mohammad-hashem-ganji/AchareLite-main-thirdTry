
@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Create</h1>

@{
    int mainCategoryId = (int)ViewData["mainCategoryId"]  ;
    int i = 0;
}

<div class="container px-5 my-5">
    <form id="contactForm" data-sb-form-api-token="API_TOKEN"
 asp-action = "CreateConfirm"   asp-controller = "SubCategory">
        <div class="form-floating mb-3">
            <input class="form-control" id="عنوان زیر دسته" type="text" placeholder="عنوان زیر دسته" data-sb-validations="required" name = "Title" />
            <label for="عنواندستهبندی">عنوان زیر دسته </label>
            <input class="form-control" type="hidden"  id=" عنوان دسته بندی اصلی" type="text" value = "@mainCategoryId" placeholder="عنوان دسته بندی اصلی" data-sb-validations="required" name = "MainCategoryId" />
            @*            <div class="invalid-feedback" data-sb-feedback="عنواندستهبندیاصلی:required">عنوان دسته بندی اصلی is required.</div>*@
        </div>
        @*        <div class="d-none" id="submitSuccessMessage">
            <div class="text-center mb-3">
            <div class="fw-bolder">Form submission successful!</div>
            <p>To activate this form, sign up at</p>
            <a href="https://startbootstrap.com/solution/contact-forms">https://startbootstrap.com/solution/contact-forms</a>
            </div>
            </div>
            <div class="d-none" id="submitErrorMessage">
            <div class="text-center text-danger mb-3">Error sending message!</div>
            </div>*@
        <div class="d-grid">
            <button class="btn btn-primary btn-lg " id="submitButton" type="submit">Submit</button>
        </div>
    </form>
</div>
<script src="https://cdn.startbootstrap.com/sb-forms-latest.js"></script>